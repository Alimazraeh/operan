apiVersion: v1
kind: Service
metadata:
  name: {{ include "policy-governance.fullname" . }}
  labels:
    {{- include "policy-governance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "policy-governance.selectorLabels" . | nindent 4 }}