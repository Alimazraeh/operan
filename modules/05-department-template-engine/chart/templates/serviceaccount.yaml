apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "module05.fullname" . }}
  labels:
    {{- include "module05.labels" . | nindent 4 }}
{{- if .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml .Values.serviceAccount.annotations | nindent 4 }}
{{- end }}
automountServiceAccountToken: false
