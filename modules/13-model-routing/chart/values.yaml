apiVersion: v2
name: model-routing
description: Module 13 — Multi-Model Routing Engine Helm chart
type: application
version: 0.1.0
appVersion: "0.1.0"

replicaCount: 1

image:
  repository: ghcr.io/operan/model-routing
  tag: latest
  pullPolicy: IfNotPresent

secretName: model-routing-secrets
m12BaseUrl: http://model-abstraction:8012

service:
  type: ClusterIP
  port: 8013